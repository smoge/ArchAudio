post_install() {
  cat <<'MSG'
=>  The extract utility is installed as csound_extract to avoid
    conflicting with the libextractor package.
MSG
}

post_upgrade() {
  if [ "$1" == "x" ]; then
    post_install $1
  fi
}

