# $Id$
# Maintainer: Arch Linux Pro Audio <dev@archaudio.org>
#Contributor: Ng Oon-Ee <n g  o o n  e e <AT g mail dot com>

pkgname=ladspa-bs2b
pkgver=0.9.1
pkgrel=2
pkgdesc="Bauer stereophonic-to-binaural DSP effect library - LADSPA plugin"
arch=('i686' 'x86_64')
url=http://bs2b.sourceforge.net
license=('GPL')
depends=('libbs2b' 'ladspa')
provides=('bs2b')
groups=('ladspa-plugins')
makedepends=()
source=("http://downloads.sourceforge.net/project/bs2b/plugins/LADSPA plugin/$pkgver/bs2b-$pkgver.tar.gz")
md5sums=('f3926a6d5effb7c70a4b69db30c186d5')

build() {
  cd ${srcdir}/bs2b-${pkgver}
  ./configure --prefix=/usr
  make
  make DESTDIR=${pkgdir} install
}
